apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atmoz-sftp.fullname" . }}-test-connection"
  labels:
    {{- include "atmoz-sftp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atmoz-sftp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
